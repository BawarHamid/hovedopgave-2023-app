import React from 'react';

type Props = {
  setPage: React.Dispatch<React.SetStateAction<number>>;
};

const SuggestedCreators: React.FC<Props> = ({ setPage }) => {
  return <div></div>;
};

export default SuggestedCreators;
